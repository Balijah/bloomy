{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "build:deploy": "pnpm run typecheck:deploy && pnpm run build:web && pnpm run build:api",
    "build:web": "pnpm --filter @workspace/bloomy-web run build",
    "install:ci": "pnpm install --frozen-lockfile",
    "set-subscription-tier": "pnpm --filter @workspace/scripts run set-subscription-tier",
    "start:api": "pnpm --filter @workspace/api-server run start",
    "start:deploy": "pnpm run start:api",
    "typecheck:deploy": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server --filter @workspace/bloomy-web --filter @workspace/bloomy-mobile --filter @workspace/scripts --if-present run typecheck",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
